# Copy to .env.local for local development.
#
# Local development — the FastAPI backend from ../backend:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production is committed in .env.production and points at the deployed Railway
# backend, so deploying needs no configuration. Override in the Vercel dashboard
# if the backend ever moves.
